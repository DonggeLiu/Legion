#!/bin/sh

# rm test/results -r
# /home/ernst/workspace/cpachecker/scripts/benchmark.py --read-only-dir / -t ReachSafety-Arrays --cloud --cloudMaster localhost legion.xml
/home/ernst/workspace/cpachecker/scripts/benchmark.py --cloudClientHeap 8000 --read-only-dir / --no-container --cloud --cloudMaster localhost "$1"
